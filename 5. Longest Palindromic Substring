class Solution {
    int start=0;
    int end=0;
    public String longestPalindrome(String s) {
        for(int i=0;i<s.length();i++)
        {
            centre(s,i,i);
            centre(s,i,i+1);
        }
        return s.substring(start,end+1);
    }
    private void centre(String s,int l,int r)
    {
        while((l>=0 && r<s.length()) && (s.charAt(l)==s.charAt(r)))
        {
            l--;
            r++;
        }
        if((r-l-1)>end-start)
        {
            start=l+1;
            end=r-1;
        }
    }
}
